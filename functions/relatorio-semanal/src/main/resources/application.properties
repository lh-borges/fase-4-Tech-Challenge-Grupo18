# ---------------------------------------------------------------------------
# relatorio-semanal — Cloud Function (GCP gen2) acionada semanalmente
# (Cloud Scheduler -> Pub/Sub "relatorio-semanal"). Busca o relatorio no backend
# (endpoint interno) e envia por e-mail ao administrador.
#
# Credenciais/segredos vem por variavel de ambiente (SMTP_PASSWORD e
# INTERNAL_API_KEY do Secret Manager). Nada sensivel versionado.
# ---------------------------------------------------------------------------

# SMTP (padroes de Gmail; sobrescreva SMTP_HOST/SMTP_PORT se usar outro provedor)
quarkus.mailer.from=${SMTP_USER}
quarkus.mailer.host=${SMTP_HOST:smtp.gmail.com}
quarkus.mailer.port=${SMTP_PORT:587}
quarkus.mailer.start-tls=REQUIRED
quarkus.mailer.username=${SMTP_USER}
quarkus.mailer.password=${SMTP_PASSWORD}
quarkus.mailer.mock=false

# Destinatario e integracao com o backend
app.admin-email=${ADMIN_EMAIL}
app.url=${APP_URL}
app.internal-api-key=${INTERNAL_API_KEY}

# Log
quarkus.log.level=INFO
quarkus.log.category."br.com.fiap.sysfeedback".level=DEBUG
